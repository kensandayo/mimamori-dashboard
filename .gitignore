# APIキーなどの秘匿情報を含むため、.env自体は共有・コミットしない
.env

# Pythonキャッシュ
__pycache__/
*.pyc

# Streamlitのローカル設定（secrets.tomlを使う場合）
.streamlit/secrets.toml

# AI相談機能の実行時データ（1日の利用回数）。実行環境ごとに変わるため共有しない
data/ai_usage.json
