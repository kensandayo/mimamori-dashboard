# このファイルを「.env」という名前でコピーし、実際のAPIキーを設定してください。
# 例： cp .env.example .env

# OpenAIのAPIキー（AI相談機能を使う場合に必須）
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 使用するモデル名（省略時は gpt-4o-mini が使われます）
OPENAI_MODEL=gpt-4o-mini

# 1日あたりのAI相談 利用回数の上限（省略時は50回）
DAILY_AI_LIMIT=50
